node_modules/
dist/
.next/
.env
*.env.local
backend/.env

# Sessions / cookies
sessions/
backend/.browser-profiles/

# Sensitive applicant data — never commit passport scans
passports/
# Decrypted passport images the worker writes per-booking — never commit
backend/.passport-cache/

# Logs (runtime artifact dirs only — NOT the src/modules/logs source module)
*.log
/logs/
/backend/logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Docker volumes
postgres-data/
redis-data/

# Test coverage
coverage/

# Playwright
playwright-report/
test-results/

# Build artifacts & temp logs
*.tsbuildinfo
frontend/tsc_errors.txt
backend/tsc_errors.txt
frontend/package-lock.json

# captured live lift-api token (never commit)
nodriver-spike/.lift-creds.json
.lift-creds.json

# Local VPS credentials / RDP access notes (never commit)
ops/local-vps-credentials.txt
